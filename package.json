{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "this is a backend service for url-shortener",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js && node --experimental-specifier-resolution=node",
    "build": "node index.js && node --experimental-specifier-resolution=node"
  },
  "author": "thedevwonder",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.8.3",
    "nodemon": "^2.0.20"
  },
  "type": "module"
}
